$terminal = ghostty
$fileManager = nautilus
$browser = firefox
$tuiPopup = $terminal --class=popup.app.thic -e

$mainMod = SUPER

$tmpWinWide = [float; center; size (monitor_w*0.5) (monitor_h*0.5)]
$tmpWinHigh = [float; center; size (monitor_w*0.3) (monitor_h*0.8)]

# Power
bind = $mainMod, X, exec, systemctl suspend
bindl = $mainMod ALT, X, exec, shutdown now
bindl = $mainMod SHIFT, X, exec, reboot

# WM Stuff
bind = $mainMod, K, exit,
bind = $mainMod, G, killactive,
bind = $mainMod, Q, killactive,
bind = $mainMod, M, fullscreen,
bind = $mainMod, C, exec, hyprlock
# bind = $mainMod, P, exec, hyprpicker
# bind = $mainMod, J, togglesplit,

# Scripts and Stuff
# TODO: flameshot doesn't make a new directory
bind = $mainMod, P, exec, flameshot gui --clipboard --path ~/pictures/screenshots
bind = $mainMod SHIFT, P, exec, flameshot gui --clipboard --last-region --accept-on-select --path ~/pictures/screenshots
bind = $mainMod, Z, exec, sh ~/scripts/toggle_screen_recording.sh
bind = $mainMod SHIFT, Z, exec, $tmpWinWide $fileManager ~/videos
bind = $mainMod ALT, L, exec, pkill waybar ; waybar ; swaync-client -rs ; pkill dunst ; dunst &
bind = $mainMod, B, exec, sh ~/scripts/bookmarks/menu.sh
bind = $mainMod SHIFT, B, exec, sh ~/scripts/bookmarks/add.sh
bind = $mainMod ALT, T, exec, sh ~/scripts/time/time.sh
bind = $mainMod, Period, exec, sh ~/scripts/stuff.sh
bind = $mainMod, Comma, exec, $tmpWinHigh $terminal -e bash -c "cd ~/notes && nvim ./notes.md"
bind = $mainMod, V, exec, cliphist list | rofi -dmenu | cliphist decode | wtype -

# Workspaces and Programs
bind = $mainMod, Space, exec, rofi -show drun
bind = $mainMod, T, workspace, 1
bind = $mainMod SHIFT, T, exec, $terminal
bind = $mainMod, F, exec, $fileManager
bind = $mainMod SHIFT, R, exec, $browser
bind = $mainMod, R, workspace, 2
bind = $mainMod, L, workspace, 4
bind = $mainMod, D, exec, discord
bind = $mainMod, W, exec, parsecd
bind = $mainMod, S, exec, $tuiPopup rmpc

# Web Apps
bind = $mainMod, I, exec, $tmpWinWide chromium --app=https://monkeytype.com/
bind = $mainMod, N, exec, $tmpWinWide chromium --app=https://search.nixos.org/packages?channel=unstable
bind = $mainMod SHIFT, N, exec, $tmpWinWide chromium --app=https://search.nixos.org/options?channel=unstable

# Move focus
bind = $mainMod, H, movefocus, l
bind = $mainMod, E, movefocus, d
bind = $mainMod, U, movefocus, u
bind = $mainMod, A, movefocus, r

# Move window
bind = $mainMod ALT, H, layoutmsg, swapcol l
bind = $mainMod ALT, E, layoutmsg, colresize -0.1
bind = $mainMod ALT, U, layoutmsg, colresize +0.1
bind = $mainMod ALT, A, layoutmsg, swapcol r

# Switch workspaces
bind = $mainMod, KP_End, workspace, 1
bind = $mainMod, KP_Down, workspace, 2
bind = $mainMod, KP_Next, workspace, 3
bind = $mainMod, KP_Left, workspace, 4
bind = $mainMod, KP_Begin, workspace, 5
bind = $mainMod, KP_Right, workspace, 6
bind = $mainMod, KP_Home, workspace, 7
bind = $mainMod, KP_Up, workspace, 8
bind = $mainMod, KP_Prior, workspace, 9
bind = $mainMod, KP_Insert, workspace, 10

# Move active window to a workspace
bind = $mainMod ALT, KP_End, movetoworkspace, 1
bind = $mainMod ALT, KP_Down, movetoworkspace, 2
bind = $mainMod ALT, KP_Next, movetoworkspace, 3
bind = $mainMod ALT, KP_Left, movetoworkspace, 4
bind = $mainMod ALT, KP_Begin, movetoworkspace, 5
bind = $mainMod ALT, KP_Right, movetoworkspace, 6
bind = $mainMod ALT, KP_Home, movetoworkspace, 7
bind = $mainMod ALT, KP_Up, movetoworkspace, 8
bind = $mainMod ALT, KP_Prior, movetoworkspace, 9
bind = $mainMod ALT, KP_Insert, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e-1
bind = $mainMod, mouse_up, workspace, e+1

bind = $mainMod SHIFT, H, workspace, e-1
bind = $mainMod SHIFT, A, workspace, e+1

bind = $mainMod, left, workspace, e-1
bind = $mainMod, right, workspace, e+1

bind = $mainMod ALT SHIFT, H, movetoworkspace, -1
bind = $mainMod ALT SHIFT, A, movetoworkspace, +1

bind = $mainMod, Tab, workspace, previous

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
bind = $mainMod, mouse:274, togglefloating

# Laptop multimedia keys for volume and LCD brightness
# l - even when locked, e - repeat when held
bindl = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2.5%+
bindl = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2.5%-
bindl = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindl = , XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

bindl = , XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindl = , XF86MonBrightnessDown, exec, brightnessctl s 10%-

bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
